Note that for some reason if we installed the rest of the modules before
regenerator, it stops regenerating.

rm -rf node_modules
npm install regenerator
npm install

Note that we changed recast/node_modules/ast_types until it's published with
our changes.